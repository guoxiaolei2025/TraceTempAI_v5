# ============================================================
# TraceTempAI 统一依赖清单（项目根目录）
# ------------------------------------------------------------
# 本文件同时覆盖前后端依赖：
#   [后端] 安装命令：pip install -r requirements.txt
#   [前端] 安装命令：cd frontend && npm install
#           （Node 依赖清单见下方说明，完整配置见 frontend/package.json）
# ============================================================

# ================= 后端 Python 依赖 =================
fastapi>=0.100.0
uvicorn>=0.20.0
python-dotenv>=1.0.0
requests>=2.30.0
python-docx>=1.1.0
openpyxl>=3.1.0
pandas>=2.0.0
pydantic>=2.0.0
python-dateutil>=2.8.0

# ================= 前端 Node.js 依赖 =================
# 前端依赖由 npm 管理，完整清单见 frontend/package.json
# 运行依赖（dependencies）：
#   vue ^3.4.21
#   element-plus ^2.6.3
#   axios ^1.6.8
#   pinia ^2.1.7
#   echarts ^5.5.0
# 开发依赖（devDependencies）：
#   vite ^5.2.8
#   @vitejs/plugin-vue ^5.0.4
#   sass ^1.72.0
#   vite-plugin-mock ^3.0.2
